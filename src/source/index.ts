import * as userRepository from './userRepository';
import * as checkInRepository from './checkInRepository';
import * as terminalRepository from './terminalRepository';
import * as tripRepository from './tripRepository';

export { userRepository, checkInRepository, terminalRepository, tripRepository };
